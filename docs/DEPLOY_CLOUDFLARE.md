# Cloudflare deploy - v36.0.0

1. Run `01-SETUP-CLOUDFLARE.bat`.
2. Run `02-LOGIN-CLOUDFLARE.bat`.
3. Run `03-DEPLOY-CLOUDFLARE.bat`.

Verify `/api/config`: `version=36.0.0`, `cards=66`, `units=59`, `spells=7`, `physicsVersion=57`, `maxDeck=8`.
