{
  "name": "tiny-siege",
  "version": "18.1.0",
  "private": true,
  "type": "module",
  "description": "Original 2D lane battler with 30 selectable cards, eight-card decks, three-column responsive deck builder, verified up to ten local My List presets, ability-focused live engine-driven card-detail battle demos, viewer-relative spell colors, persistent touch building placement, CPU practice and private 1v1 on Cloudflare Workers.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server/dev.mjs",
    "dev": "wrangler dev --port 8787",
    "login": "wrangler login",
    "whoami": "wrangler whoami",
    "deploy": "wrangler deploy",
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:network": "node tests/network.mjs",
    "build:offline": "node scripts/build-offline.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
